{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "echo \"APP_MODE=$EXPO_PUBLIC_APP_MODE\" && npx expo export --platform web",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
